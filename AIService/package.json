{
  "name": "AIService",
  "version": "1.0.0",
  "description": "This service will receive messages with namespace and find the answer to the question",
  "main": "./dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tsc -b && node --env-file=.env dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.21",
    "typescript": "^5.5.2"
  },
  "dependencies": {
    "@langchain/core": "^0.2.11",
    "@langchain/openai": "^0.2.1",
    "@langchain/pinecone": "^0.0.7",
    "@pinecone-database/pinecone": "^2.2.2",
    "documents": "link:@langchain/core/documents",
    "express": "^4.19.2"
  }
}
